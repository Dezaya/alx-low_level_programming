#include "main.h"
/**
 * _abs - print the absolute value of an integer
 * @t: the interger to be printed
 * return: the absolute value of int
 */
int _abs(int t)
{
	if (t >= 0)
	{
		return (t);
	}
	return (-t);
}
