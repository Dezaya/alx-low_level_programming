#include "main.h"
/**
 * _abs -computes the absolute value of an integer
 * @t: the interger to b compute
 * Return: The absolute value
 */
int _abs(int t)
{
	if (t >= 0)
		return (t);
	else
		return (-t);
}
